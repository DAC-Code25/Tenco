# Tenco 项目开发代理规范

## 1. 适用范围与优先级

本文件适用于仓库根目录及全部子目录，是本项目新增功能、功能改进、代码重构、缺陷修复、构建调整和相关文档修改时必须遵守的工作规范。

执行优先级如下：

1. 用户在当前任务中的明确要求。
2. 本文件的项目规范。
3. 仓库内其他开发文档和通用工具默认行为。

若要求之间存在冲突、现有修改归属不清或继续操作可能覆盖用户工作，必须停止有风险的写操作，说明事实并等待确认。不得为了满足流程而删除、丢弃、隐藏或覆盖已有修改。

## 2. 项目速览

Tenco 是基于 Qt 6 Widgets 和 C++20 的机器人/底盘上位机，使用 CMake + Ninja 构建。主要能力包括：

- HTTP 状态轮询与运行状态展示。
- WebSocket 底盘控制、手动输入和运动命令安全仲裁。
- MJPEG/OAK 视频源、预览、截图和录像。
- 地图编辑、路径规划、路线分段派发和多垄作业。
- `config.json` 配置管理、SQLite 本地存储、结构化日志和诊断导出。
- 维护、帮助和关于页面。

主要 CMake 模块：

- `tenco_config`：配置加载、校验和坐标参数。
- `tenco_logging`：日志、审计、脱敏和轮转。
- `tenco_storage`：本地数据库。
- `tenco_motion`：路线跟随、寻路和运动仲裁。
- `tenco_map_model`：地图文档、几何、路线和作业数据模型。
- `tenco_network`：状态、底盘、云台、相机和作业网络客户端。
- `tenco_video`：视频源抽象、MJPEG/OAK 后端和帧工作线程。
- `Tenco`：Qt Widgets 主程序和页面协调层。

修改前应优先阅读：

- `README.md`
- `docs/README.md`
- `docs/02_Build_and_Run.md`
- `docs/04_Architecture.md`
- `docs/07_Dev_Workflow.md`
- `docs/08_Testing_and_CI.md`
- 与任务直接相关的 `docs/develop/` 专题方案

## 3. 总体强制原则

1. 一项功能、改进、重构或修复对应一条独立分支、一份新的开发方案和一份新的开发记录。
2. 任何代码修改前必须先完成 Git 基线检查、远程同步和开发方案；不得先改代码后补方案。
3. 方案必须同时考虑性能、稳定性、可靠性、兼容性、安全性、可测试性和无关模块保护。
4. 以满足需求的最小改动取得最佳效果；不夹带无关重构、批量格式化、依赖升级或行为改变。
5. 不得破坏用户已有修改，不得使用 `git reset --hard`、强制检出覆盖、无依据删除等破坏性方式清理工作区。
6. 修改完成后必须配置、构建并测试；发现问题必须继续修复和复测，不能把已知失败作为完成结果交付。
7. 所有新 Commit 的标题和正文说明、PR 标题和 PR 描述必须使用中文。`feat`、`fix`、`refactor`、`docs`、`test`、`build`、`chore` 等类型前缀可以保留，冒号后的描述必须为中文。
8. 不得声称已经提交、推送、创建 PR 或通过测试，除非对应命令确实成功并有可核验结果。

## 4. 修改前的 Git 基线门禁

### 4.1 检查未保存和未提交内容

开始任务时必须先执行并记录：

```powershell
git status --short --branch
git diff --check
git diff
git diff --cached
git branch --show-current
git remote -v
git log -5 --oneline --decorate
```

终端只能看到已经写入磁盘的文件，无法可靠识别 Qt Creator 尚未保存的编辑器缓冲区。因此：

- 能控制 Qt Creator 时，先执行“全部保存”。
- 只能使用终端时，在切换分支前明确提醒用户保存 Qt Creator 中的未保存内容；若用户已说明存在未保存内容，必须等待保存后再继续。
- Git 状态只能表述为“磁盘工作区干净”，不能把它误称为已经检查到所有 IDE 缓冲区。

### 4.2 发现已有修改时

若存在未提交、暂存、未跟踪或未推送的内容：

1. 先查看差异、文件归属、当前分支和上游关系，不得直接覆盖、还原或换分支。
2. 若明确属于上一项已经完成的开发或修复：补全对应开发记录，按其方案完成构建与测试，使用中文说明提交，并推送当前分支。
3. 当前分支存在尚未交付的提交时，先推送，并创建 PR；环境无法创建 PR 时，必须给出可直接使用的中文 PR 标题和完整描述。
4. 若修改归属不明、可能包含用户正在进行的工作、机密文件或未完成内容，停止提交和分支切换，列出发现并请用户确认。
5. 不得把多项不相关的历史修改打包进当前任务，不得未经确认使用 `git stash` 隐藏问题。

若工作区干净但当前分支存在未推送提交，也必须先推送并处理 PR。若当前分支已合并且远程分支已删除，只需记录该事实，不创建空提交或重复 PR。

### 4.3 同步最新基线

已有工作妥善交付且磁盘工作区干净后，执行：

```powershell
git fetch <远程名> --prune
git remote show <远程名>
git switch <远程默认分支>
git pull --ff-only <远程名> <远程默认分支>
```

- 本仓库当前远程名为 `Tenco`、默认分支为 `master`；仍应在执行时复核，不能永久假设远程配置不变。
- 只允许快进同步。若本地主分支分叉，不得擅自 merge、rebase 或强推，必须先说明情况。
- 同步后再次执行 `git status --short --branch`，确认干净且与远程一致。

### 4.4 创建任务分支

每项新工作必须从最新默认分支创建新分支，不复用上一任务分支：

```powershell
git switch -c <类型>/<简短主题>-v<版本>
```

建议类型：

- `feature/`：新增功能。
- `fix/`：缺陷修复。
- `refactor/`：保持行为不变的重构。
- `perf/`：性能优化。
- `docs/`：文档与流程治理。
- `build/`：构建、依赖或 CI 调整。

分支名为兼容工具和远程环境可使用简洁英文；分支对应的方案、记录、提交和 PR 描述仍必须使用中文。

## 5. 修改前的开发方案门禁

### 5.1 创建位置与命名

新增功能、功能改造、重构和缺陷修复在修改代码前，必须在 `docs/develop/` 新建一份独立方案：

```text
docs/develop/<中文任务名称>开发方案.md
```

文件名必须以中文为主体。Qt、C++、HTTP、WebSocket、USB、OAK、MJPEG、V1、V2 等不可替代的技术名称可以保留。任务名重复时增加日期或版本号，禁止覆盖旧方案来代替新方案。

### 5.2 方案必填内容

方案至少包含：

1. 日期、需求类型、基线、当前分支。
2. 背景、现状和问题证据。
3. 目标、非目标和验收标准。
4. 候选方案、选择理由和取舍。
5. 预计修改的文件、模块、接口、配置和数据结构。
6. 对性能、稳定性、可靠性、安全性、兼容性和无关模块的影响。
7. 线程安全、对象生命周期、资源释放、异常和失败恢复策略。
8. 最小化修改策略、风险、回滚办法。
9. 单元、集成、构建、GUI、设备联调和回归测试计划。
10. 分阶段实施步骤。

方案完成并自检通过后才能修改业务代码。实施中如果发现原方案不可行或范围发生实质变化，必须先更新方案和原因，再继续开发。

## 6. 开发实施规范

1. 先定位根因和现有调用链，再修改；不得通过掩盖错误、吞异常或扩大超时假装修复。
2. 优先复用现有模块和接口，保持公开 API、配置格式、地图/任务文件和网络协议向后兼容。
3. Qt UI 只能在 UI 线程更新；跨线程 QObject 必须明确所有权、信号槽连接方式、停止顺序和析构安全。
4. 网络和设备操作必须考虑超时、取消、重连退避、重复请求、离线状态和资源释放，不能阻塞 UI 线程。
5. 运动控制相关修改必须保证急停优先、命令互斥、失联停止和失败可恢复，不得削弱 `MotionCommandArbiter` 的安全边界。
6. 配置项应进入 `config.json`/`ConfigManager` 并提供校验、默认值和兼容策略；不得散落环境相关硬编码。
7. 日志不得泄露口令、令牌、个人信息或完整敏感载荷；使用现有日志分类、脱敏和审计能力。
8. 新增算法和业务状态优先下沉到可单测的非 UI 模块，避免继续扩大 `home.cpp`、`map.cpp` 等大型协调文件。
9. 只修改与方案直接相关的文件。若必须扩大范围，先在方案和开发记录中说明理由与影响。
10. 保持 UTF-8、现有 C++20/CMake/Qt 6 风格；不做无关的大面积格式化。

## 7. 每项任务的独立开发记录

### 7.1 创建位置与命名

每次新增、改进、重构或修复必须新建一份记录，不得复用旧记录：

```text
docs/develop/<中文任务名称>开发记录.md
```

名称重复时增加日期或版本号。开发记录与方案一起进入同一任务分支。

### 7.2 记录必填内容

开发记录至少包含：

- 任务名称、类型、日期、基线分支、开发分支。
- 对应方案文件。
- 修改前 Git 状态和远程同步结果。
- 本次新增、修改、删除的内容及涉及文件。
- 为什么这样修改，采用方案及关键取舍。
- 对功能、性能、稳定性、可靠性、安全性、兼容性和无关模块的实际影响。
- 构建命令、测试命令、测试数量、通过/失败结果和失败后的修复过程。
- 未能自动执行的 GUI/设备测试、原因、风险和人工验证清单。
- 方案符合性复核结果和已知限制。
- Commit 哈希与中文说明。
- 远程名、推送分支、推送日期和结果。
- PR 链接；若无法创建，则记录已准备的中文 PR 标题与描述。

记录必须随开发过程持续更新，禁止在未执行验证时预填“已通过”。提交哈希、推送和 PR 信息应在对应动作成功后补记；为补齐交付元数据允许追加一个独立中文文档提交。

## 8. 固定构建环境

后续修改完成后的本机构建验证必须优先使用用户在 Qt Creator 中确认的以下 Kit，不得随意切换到其他 Qt 或编译器后声称验证完成：

| 项目 | 固定值 |
|---|---|
| 源码目录 | `D:/QTProject/Tenco` |
| 构建目录 | `D:/QTProject/Tenco/build/Desktop_Qt_6_5_3_MinGW_64_bit-Debug` |
| Qt | `D:/Qt/6.5.3/mingw_64` |
| Kit | Desktop Qt 6.5.3 MinGW 64-bit |
| C 编译器 | `D:/Qt/Tools/mingw1120_64/bin/gcc.exe` |
| C++ 编译器 | `D:/Qt/Tools/mingw1120_64/bin/g++.exe` |
| CMake | `D:/Qt/Tools/CMake_64/bin/cmake.exe` |
| Ninja | `D:/Qt/Tools/Ninja/ninja.exe` |
| 生成器 | Ninja |
| 构建类型 | Debug |
| CTest 总开关 | `BUILD_TESTING=ON` |
| 项目测试开关 | `TENCO_BUILD_TESTS=ON` |
| QML 调试 | 启用，`CMAKE_CXX_FLAGS=-DQT_QML_DEBUG` |

标准 PowerShell 配置命令：

```powershell
$env:PATH = 'D:/Qt/6.5.3/mingw_64/bin;D:/Qt/Tools/mingw1120_64/bin;D:/Qt/Tools/Ninja;' + $env:PATH

& 'D:/Qt/Tools/CMake_64/bin/cmake.exe' `
  -S 'D:/QTProject/Tenco' `
  -B 'D:/QTProject/Tenco/build/Desktop_Qt_6_5_3_MinGW_64_bit-Debug' `
  -G Ninja `
  -DCMAKE_MAKE_PROGRAM:FILEPATH='D:/Qt/Tools/Ninja/ninja.exe' `
  -DCMAKE_BUILD_TYPE:STRING=Debug `
  -DBUILD_TESTING:BOOL=ON `
  -DTENCO_BUILD_TESTS:BOOL=ON `
  -DCMAKE_PREFIX_PATH:PATH='D:/Qt/6.5.3/mingw_64' `
  -DCMAKE_C_COMPILER:FILEPATH='D:/Qt/Tools/mingw1120_64/bin/gcc.exe' `
  -DCMAKE_CXX_COMPILER:FILEPATH='D:/Qt/Tools/mingw1120_64/bin/g++.exe' `
  '-DCMAKE_CXX_FLAGS:STRING=-DQT_QML_DEBUG'
```

标准构建命令：

```powershell
& 'D:/Qt/Tools/CMake_64/bin/cmake.exe' `
  --build 'D:/QTProject/Tenco/build/Desktop_Qt_6_5_3_MinGW_64_bit-Debug' `
  --parallel
```

标准测试命令：

```powershell
& 'D:/Qt/Tools/CMake_64/bin/ctest.exe' `
  --test-dir 'D:/QTProject/Tenco/build/Desktop_Qt_6_5_3_MinGW_64_bit-Debug' `
  --output-on-failure
```

规则：

1. 配置、完整构建和 CTest 三步都必须执行。
2. 不能只编译被修改的单个目标来代替完整构建。
3. 构建目录或缓存损坏时，先记录错误和原因，再使用安全方式修复；不得删除范围不明的目录。
4. 只有在固定 Kit 客观不可用时，才可使用其他环境做补充诊断；必须明确标记为“非固定 Kit 验证”，不能替代固定 Kit 验收。
5. Qt Creator 中继续保持 Debug、QML debugging and profiling Enable、`BUILD_TESTING=ON` 和 `TENCO_BUILD_TESTS=ON`。
6. 命令行运行 CTest 前必须保留上面的 `PATH` 注入，使测试进程能加载 Qt 6.5.3 与 MinGW 11.2 运行时；若出现 `0xc0000135`，先检查运行时路径，不能误判为测试断言失败。

## 9. 测试与完成门禁

### 9.1 必做检查

每次修改完成后至少执行：

```powershell
git diff --check
git status --short --branch
git diff --stat
```

并按第 8 节执行固定环境的 CMake 配置、完整构建和 CTest。

### 9.2 按改动补充测试

- 算法、解析、配置、数据库和状态机：新增或更新单元测试，覆盖正常、边界、非法输入和失败恢复。
- 模块交互：补充集成测试，验证信号槽、队列、线程和数据流。
- UI：执行启动、页面切换、关键交互、窗口缩放和异常提示的冒烟测试。
- 网络/设备：覆盖在线、离线、超时、断线重连、重复操作和安全停止。
- 文档/路径：全仓检索旧路径，检查链接和帮助页引用存在。

### 9.3 方案符合性复核

测试后必须逐项对照开发方案：

1. 需求和验收标准是否全部满足。
2. 是否出现超出方案的文件或行为变化。
3. 是否影响无关模块、性能、稳定性、可靠性或兼容性。
4. 风险与限制是否已进入开发记录。

任何必需构建或测试失败都不允许直接提交为完成状态。应定位、修复、重新构建和复测，直到通过；若受外部设备、权限或服务阻塞，必须提供可复现证据和剩余人工验证步骤。

## 10. 提交、推送与 PR

### 10.1 提交前

1. 审查 `git diff` 和 `git diff --cached`，确认没有机密、构建产物和无关修改。
2. 确认开发方案与开发记录完整，实际结果与记录一致。
3. 确认固定 Kit 构建和测试已通过。
4. 提交只聚焦当前任务；必要时拆分为可解释的多个中文提交。

### 10.2 中文提交规范

推荐格式：

```text
<类型>: <中文简述>
```

示例：

```text
feat: 新增地图路线断点续作能力
fix: 修复视频断线后重复创建工作线程的问题
refactor: 抽离首页输入状态并补充单元测试
docs: 建立项目开发治理规范并统一方案文件名
```

如需正文，背景、修改、影响和验证说明同样全部使用中文。

### 10.3 推送与 PR

本机标准 PR 工具为 GitHub CLI。每次远程交付前先验证：

```powershell
gh --version
gh auth status
```

若认证失效，使用 GitHub 官方网页设备授权恢复登录；不得把令牌、验证码或凭据写入仓库、开发记录或命令历史。Git 继续使用仓库现有协议，不擅自改写远程地址或 SSH 密钥。

提交后执行：

```powershell
git push -u <远程名> <当前分支>
gh pr view <当前分支> --json number,url,state,title
```

- 推送成功后更新开发记录中的分支、日期、结果和提交信息。
- 若 `gh pr view` 表明当前分支没有 PR，必须自动执行 `gh pr create --base <默认分支> --head <当前分支> --title "<中文标题>" --body "<中文描述>"`。
- 若 PR 已存在，使用 `gh pr edit` 更新中文标题和描述，不重复创建。
- 创建或更新成功后，必须用 `gh pr view` 核验 PR 编号、URL、状态、基准分支和来源分支，并写入开发记录。
- 只有 GitHub 服务、网络或认证客观不可用时，才允许退化为提供 Compare 链接和完整中文 PR 内容；必须记录失败证据，不得把退化方案当作正常完成流程。
- 不得强推默认分支，不得绕过 PR 直接把任务提交推到默认分支。

中文 PR 描述至少包含：

```markdown
## 背景

## 本次修改

## 设计与取舍

## 影响范围

## 风险与兼容性

## 构建与测试

## 人工验证项

## 对应文档
```

## 11. 完成交付清单

只有以下项目全部满足，任务才可以报告完成：

- [ ] 修改前基线检查已完成，旧工作已妥善保存、提交、推送和处理 PR。
- [ ] 已从最新远程默认分支创建独立任务分支。
- [ ] 修改代码前已创建中文开发方案。
- [ ] 已创建本次独立中文开发记录。
- [ ] 实现遵循最小改动并保护无关模块。
- [ ] 固定 Qt 6.5.3 Kit 配置成功。
- [ ] 主程序和全部测试目标完整构建成功。
- [ ] CTest 全部通过，必要的专项测试已完成或已明确列出人工项。
- [ ] 已对照方案完成复核并记录影响、风险和限制。
- [ ] Commit 标题和正文使用中文。
- [ ] 新分支已推送 GitHub。
- [ ] PR 已创建，或已提供完整中文 PR 标题与描述。
- [ ] 最终交付说明包含分支、提交、测试结果、PR 状态和文档位置。
