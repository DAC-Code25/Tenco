# 位姿地图全链路可靠性开发记录

日期：2026-09-19。类型：修复与集成验证。分支：fix/pose-map-chain-v1。
方案：位姿地图全链路可靠性开发方案.md。

## 基线

修改前两仓库磁盘工作树干净，fetch/prune 完成；从最新默认分支建立新分支，快进纳入上一轮已推送的独立通道实现。Tenco 基线 88783bd，src 基线 dec5969。没有切换工具链或操作 D:/QTProject/Tenco。

## 实施与验证

已修复地图车头方向、单垄方向箭头、批量点方向文字；车辆图形逐条跟随有效位姿，点邻近查找仍节流。移除一次重复规划指纹刷新，视图旋转等展示变更不使任务失效。
PoseClient 与共享配置使用相同 350 ms 状态新鲜度；原始观测退化不额外阻止合法位姿。非法位置保留暗显的最后位置，不能冒充当前有效位置。
回读计划显示真实路径和步骤，检查坐标绑定、任务/版本、boot/execution、状态新鲜、回读缓存内容及明确确认；重开后只恢复计划认知，明确继续才恢复运动。服务 planHash 作为不透明内容身份，客户端不独立复算 JsonCpp 哈希。

红绿证据：车头/单垄箭头真实图形断言、视图旋转、回读接续、降级可用性、逐条小位移显示均纳入回归。单垄五方向测试旧代码五项失败，修复后通过；跨仓动态检查先检出原有 2 cm/120 ms 显示滞后，修复后实际图形与模型一致。

固定 Qt 6.5.3 / MinGW 11.2、Debug/QML 调试，当前工作空间独立目录：

```powershell
cmake -S . -B build/ipc-task-client-v1-Qt6.5.3-Debug -G Ninja -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=ON -DTENCO_BUILD_TESTS=ON -DCMAKE_PREFIX_PATH=D:/Qt/6.5.3/mingw_64
cmake --build build/ipc-task-client-v1-Qt6.5.3-Debug --parallel 4
ctest --test-dir build/ipc-task-client-v1-Qt6.5.3-Debug --output-on-failure
python tests/run_ipc_integration.py --ipc-source ../src --server ../src/build/control-windows/tracking_sim_server.exe --client build/ipc-task-client-v1-Qt6.5.3-Debug/tenco_ipc_client_probe.exe
python tests/run_gui_smoke.py --app ../bin/Tenco.exe --output build/gui-smoke-pose-chain
```

配置、全部目标构建成功；最新 CTest 16/16 通过（12.79 s）。8 个原生界面截图和退出冒烟通过，已查看地图常规/小窗口截图。跨仓探针使用真实 Map/PoseClient/TaskCompiler/TrackingClient 和真实 C++ 服务，覆盖非零平移旋转、示教、上传、执行、暂停、客户端重开、远端路径弹窗确认、继续、拍摄确认、Map 路线再上传和 Qt 退出后工控机自主完成，全部通过。
该样例模型误差 8.88e-16 m、图形位置误差 1.14e-15 m，表示软件坐标数学一致性，不是实车定位精度。桌面回环控制周期 p99 64.06 ms、计算 p99 0.085 ms，210 次计算样本且 deadlineMisses=0，仅为本次短时桌面测量。
独立复核已完成，所发现方向缺陷已修复。Linux 真实传感器→UKF→ROS→tracking 的最终结果随配套仓库 CI 补记；不提前宣称整套验证完成。
遵守 Lua 不变、旧 WebSocket 手动直通与工控机自主执行独立的约定。

大型任务回读补充：原 HTTP 客户端统一 1 MiB 响应限制导致合法大型任务无法回读接续。先用含五万路径点、超过 1 MiB 的真实 HTTP 响应扩展接续回归，旧实现收到任务次数为 0（12 项通过、1 项失败）；随后给请求增加有界响应大小参数，仅任务回读使用与服务文档一致的 16 MiB 上限，其他请求默认仍为 1 MiB，参数本身也不能超过 16 MiB。补充默认超限拒绝和试图将上限增至 32 MiB 时仍拒绝超过 16 MiB 响应的回归。固定 Kit 目标 `tenco_test_ipc_clients` 构建成功，`ctest --test-dir build/ipc-task-client-v1-Qt6.5.3-Debug -R '^tenco_test_ipc_clients$' --output-on-failure` 1/1 通过（9.95 s）；本补充之后再次完整构建、CTest 16/16（12.79 s）、跨仓 Map/服务联调和 GUI 冒烟均已通过。

## 交付

阶段提交 abe2717 已推送 Tenco/fix/pose-map-chain-v1，PR https://github.com/DAC-Code25/Tenco/pull/17 已创建（Draft）。该阶段云端 Qt 6.8.3 Debug/Release 均通过，run 35438256327；大任务回读补充提交及其云端结果后续补记。未连接实车，未验证物理标定、绝对定位精度、目标机长时间时延；小屏界面现有控件拥挤仍属于后续 UI 优化，不影响本轮坐标链路断言。
